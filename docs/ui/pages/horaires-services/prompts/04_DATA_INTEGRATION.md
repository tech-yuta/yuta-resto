# Codex Prompt — Phase 4: Approved Domain Extension

Do not run this phase unless a proposed capability in `PRODUCT_SCOPE.md` has explicit approval.

Before code, provide:

- approved user behavior;
- current field mapping;
- missing contract and schema capability;
- permission impact;
- enum and constraint impact;
- migration or development reset impact;
- locale and timezone behavior;
- cache and revalidation impact;
- test plan;
- documentation paths to update.

Stop and wait for approval.

After approval, implement the smallest coherent extension while preserving trusted organization and establishment scope, server boundaries, canonical time representation, and current booking behavior.

Run affected auth, tenant, contract, booking, cloud-database, Backoffice, documentation, architecture, and build checks.
