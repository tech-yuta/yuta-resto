# Codex Prompt — Phase 3: Current Interaction Refinement

Improve only current approved interactions on `/etablissement/horaires-services`.

Potential scope:

- clearer pending and persisted-success state;
- field and form validation presentation;
- exception deletion confirmation;
- focus management;
- accessible action menus;
- exception-kind dependent fields;
- retry and recovery;
- mobile form presentation.

Preserve trusted server scope and current mutation behavior.

Do not implement:

- copy-day;
- day-level enabled persistence;
- unsaved public preview;
- per-service reservation windows;
- last arrival;
- per-service duration;
- a new exception kind;
- a new state or form library.

Run interaction, keyboard, focus, mobile, test, and build verification. Report exact results.
