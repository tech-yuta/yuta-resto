# Required — connection string used by the Next.js app
DATABASE_URL=postgres://yuta:change_me@localhost:5432/luna_display

# Required — Postgres password (used by docker-compose for the db service)
POSTGRES_PASSWORD=change_me

# Optional — defaults to ./public/uploads/display when unset
# Set to an absolute path when running in Docker
UPLOAD_DIR=
